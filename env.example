# Environment variables (example)
#
# For local development:
# - Copy `apps/app/env.example` -> `apps/app/.env.local`
# - Copy `apps/web/env.example` -> `apps/web/.env.local`
#
# Convex has its own environment variables (set via Convex Dashboard or CLI).
# See: `apps/app/convex/env.example`

# ---------------------------------------------------------------------------
# Shared / tooling (CI / deploy)
# ---------------------------------------------------------------------------

# Convex deploy key (used by Convex CLI in CI/CD, not by the Next.js runtime).
CONVEX_DEPLOY_KEY=


