# Example environment variables for `apps/app` (Next.js Email Builder)
#
# Copy to: `apps/app/.env.local`
# Never commit real secrets.

# Convex deployment URL used by the browser + Next.js routes.
NEXT_PUBLIC_CONVEX_URL=https://<your-deployment>.convex.cloud

# Public URL of the builder app (used for absolute URLs).
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Required for the AI assistant API route (Vercel AI Gateway).
AI_GATEWAY_API_KEY=

# Optional: enables sending emails from the Next.js route at `/api/send-email`.
# If empty, the route will log emails instead of sending.
RESEND_API_KEY=


