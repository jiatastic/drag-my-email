# Example Convex environment variables for `apps/app/convex`
#
# Convex environment variables are NOT loaded from `.env.local`.
# Set them via:
# - Convex Dashboard: Settings → Environment Variables
# - Or CLI: `npx convex env set NAME value`
#
# Never commit real secrets.

# Used by Convex Auth config (`apps/app/convex/auth.config.ts`).
# For local dev, this is typically the Next.js app origin.
CONVEX_SITE_URL=http://localhost:3000

# Required by Convex Auth to sign JWTs.
# Generate one with: `cd apps/app && bun run generate-jwt-keys`
JWT_PRIVATE_KEY=

# Required for email verification / password reset OTPs.
RESEND_API_KEY=

# Sender email address (optionally include a display name, e.g. `Your App <no-reply@yourdomain.com>`).
RESEND_FROM=no-reply@yourdomain.com

# Optional: enables brand import via Firecrawl on `/assets`.
FIRECRAWL_API_KEY=

# Optional: enables marketing image generation via fal.ai on `/assets`.
FAL_KEY=


