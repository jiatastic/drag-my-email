# Example environment variables for `apps/web` (Next.js marketing / waitlist)
#
# Copy to: `apps/web/.env.local`
# Never commit real secrets.

# URL of the builder app (used in the marketing site).
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: required to enable `/api/waitlist` signup via Resend Audience.
RESEND_API_KEY=
RESEND_AUDIENCE_ID=


